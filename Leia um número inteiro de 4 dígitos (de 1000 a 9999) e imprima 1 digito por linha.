# 47 - Leia um número inteiro de 4 dígitos (de 1000 a 9999) e imprima 1 digito por linha.

n = int(input('Informe um número entre 1000 à 9999: '))

n_str = str(n)

for caractere in n_str:
    print(caractere)
